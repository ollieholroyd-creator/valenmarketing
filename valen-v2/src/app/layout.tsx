import type { Metadata } from "next";
import "./globals.css";
import { Navbar } from "@/components/layout/Navbar";
import { Footer } from "@/components/layout/Footer";

export const metadata: Metadata = {
  title: {
    default: "VALEN — Find Every Product I've Recommended",
    template: "%s | VALEN",
  },
  description:
    "Discover every product I've recommended across TikTok, Instagram, YouTube, Pinterest and Facebook. Curated reviews, honest scores, and direct links.",
  keywords: ["product recommendations", "affiliate", "reviews", "tech", "kitchen", "fitness"],
  openGraph: {
    title: "VALEN",
    description: "Find every product I've recommended across my social media.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <head>
        <link rel="preconnect" href="https://fonts.googleapis.com" />
        <link rel="preconnect" href="https://fonts.gstatic.com" crossOrigin="anonymous" />
        <link
          href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800;900&display=swap"
          rel="stylesheet"
        />
      </head>
      <body className="bg-background text-text antialiased min-h-screen flex flex-col">
        <Navbar />
        <main className="flex-1">{children}</main>
        <Footer />
      </body>
    </html>
  );
}
